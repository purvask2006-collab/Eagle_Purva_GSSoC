# Tracking settings (Phase 2)
TRACKER_MAX_AGE=30          # frames before a lost track expires
TRACKER_N_INIT=3            # frames before a track is confirmed
TRACKER_MAX_COSINE=0.4      # appearance similarity threshold (0–1)
TRACKER_FPS=30